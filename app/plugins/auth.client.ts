// Инициализация авторизации происходит в index.vue
// Плагин больше не нужен, но оставляем для совместимости
export default defineNuxtPlugin(() => {
	// Инициализация теперь происходит последовательно в index.vue
});

