export default defineNuxtPlugin(async () => {
	// Плагин для настройки базового URL API запросов
	// В случае с Tauri, когда приложение загружается с внешнего URL через WebviewUrl::External,
	// все относительные запросы автоматически идут к этому серверу.
	// Этот плагин может быть использован для дополнительной настройки в будущем.
});

